# Server configuration
server.port=8080
server.servlet.context-path=/api

# Spring configuration
spring.application.name=CarManagement

# Enable Servlet registration
spring.main.allow-bean-definition-overriding=true